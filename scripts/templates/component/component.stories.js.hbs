import { storiesOf } from '@storybook/react'
import React from 'react'
import { storyConfig, storyConfigHelpers } from '../../../.storybook/storyConfig'
import {{ pascalCase name }} from './{{ pascalCase name }}'

const config = storyConfigHelpers.getConfig({ name: '{{ pascalCase name }}' })

function {{ pascalCase name }}Demo() {
  return <{{ pascalCase name }} text="TODO: {{ pascalCase name }}" />
}

storiesOf('{{ pascalCase name }}', module).add('{{ pascalCase name }}', {{ pascalCase name }}Demo, config)